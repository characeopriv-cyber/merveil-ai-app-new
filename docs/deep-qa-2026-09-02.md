# Merveil AI — Deep Production QA

Date: 2026-09-02

## Scope

Repository inspection, production deployment verification, API health checks, runtime error review, RLS/policy review, privileged-function review, and first-party feature architecture review.

## Production

- Deployment `dpl_3HmppeUdJabfcvxyYMDr34sAR1g1`: READY.
- Build completed successfully.
- Current deployment runtime scan: no error/fatal application logs.
- Last-24-hour 5xx request-path scan: no 5xx paths reported.
- Main application deployment URL: HTTP 200.
- `/api/v1/health`: HTTP 200, service operational.
- PWA manifest is served successfully.
- No unresolved Vercel toolbar feedback threads.

## API

Verified v1 router/catalog architecture for health, profile, Passport, verification, Connect, messages, AI, Call, companies, properties, World, investors, credits, webhooks, OAuth, usage, apps, organization, billing, and developer configuration.

Commercial API architecture is present for onboarding, organizations, subscriptions, usage, invoices and billing events. Live payment capture is not claimed without a verified payment transaction.

## Security

All inspected public-schema tables have RLS enabled.

Privileged SECURITY DEFINER functions inspected were not executable by `anon` or `authenticated` roles.

Three ownership-policy gaps were found and corrected directly in production:

1. `profiles` update policy now requires `auth.uid() = id` in both `USING` and `WITH CHECK`.
2. `properties` update policy now requires `auth.uid() = owner_id` in both `USING` and `WITH CHECK`.
3. `world_posts` update policy now requires `auth.uid() = owner_id` in both `USING` and `WITH CHECK`.

The corrected policies were re-read from PostgreSQL and verified present.

## Feature architecture reviewed

- Passport / profile identity layer
- Connect and participant-authorized messaging
- Presence
- World Reels
- Pulse property reels
- Creator Studio
- Arena and Arena audio controls
- Credits and credit ledger
- Date Me
- AI Call
- Developer API platform
- Webhooks
- OAuth
- Commercial billing/onboarding

## Arena

Arena audio lifecycle hardening is deployed for the Arena HTML experiences. Existing game controls expose volume and mute state. Physical audio playback still requires device/browser testing because autoplay and OS media policies cannot be fully validated from server-side inspection.

## Remaining manual/device QA

The following cannot be conclusively certified from server-side tooling alone:

- Google/Apple/phone/email sign-in flows with a real user account
- Passport camera/document capture
- Real microphone/camera permissions
- WebRTC call establishment on two devices
- AI Call voice/language/native-speech playback
- Arena audio playback after background/foreground transitions on Android and iOS
- Push notification delivery to physical devices
- Real-time Connect presence across separate clients
- Reel camera/video upload and playback on physical mobile devices
- Marketplace upload flows using actual media
- End-to-end OAuth consent with live third-party credentials
- Live payment capture with a real provider transaction

## Runtime warning

A Node `DEP0169` deprecation warning (`url.parse()`) appears in runtime telemetry and has accumulated without corresponding application errors. It should be removed during dependency/runtime cleanup. It is currently classified as a warning, not a production request failure.

## QA status

Infrastructure/API/security: PASS after fixes.

Production deployment: PASS.

Full interactive device certification: NOT YET CERTIFIED.
