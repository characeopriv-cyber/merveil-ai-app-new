# Cloudflare Production Deployment

This commit intentionally triggers the production Cloudflare Pages deployment from `main`.

The production workflow deploys the Vite build to the Cloudflare Pages project `Merveil`.
