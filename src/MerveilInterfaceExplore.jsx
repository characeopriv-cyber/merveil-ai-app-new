import React, { useEffect, useMemo, useState } from "react";

const CATEGORIES = ["All", "Platforms", "Developer", "Interfaces", "Services", "New"];
const ICONS = { Platforms: "M", Developer: "⌘", Interfaces: "◈", Services: "✦", New: "✧" };

export default function MerveilInterfaceExplore() {
  const [items, setItems] = useState([]), [filter, setFilter] = useState("All"), [query, setQuery] = useState(""), [loading, setLoading] = useState(true), [message, setMessage] = useState("");
  async function load() {
    setLoading(true);
    try {
      const r = await fetch("/api/interface-discovery?limit=50", { credentials: "include" });
      const j = await r.json();
      if (!r.ok) throw new Error(j.error || "Unable to load discovery");
      setItems(Array.isArray(j.items) ? j.items : Array.isArray(j.interfaces) ? j.interfaces : []);
    } catch (e) {
      setMessage(e.message === "Authentication required" ? "Sign in to Merveil to personalize discovery." : "Discovery is temporarily unavailable.");
    } finally { setLoading(false); }
  }
  useEffect(() => { load(); }, []);
  const visible = useMemo(() => items.filter(x => {
    const c = x.discovery_category || "Platforms";
    const h = `${x.name || ""} ${x.slug || ""} ${x.description || ""}`.toLowerCase();
    return (filter === "All" || c === filter || (filter === "New" && x.is_new)) && (!query || h.includes(query.toLowerCase()));
  }), [items, filter, query]);
  async function event(item, type) {
    try { await fetch("/api/interface-discovery?action=event", { method: "POST", credentials: "include", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ interface_id: item.interface_id || item.id, event_type: type, source: "discover" }) }); } catch {}
  }
  function open(item) {
    event(item, "interaction");
    if (item.route) window.location.href = item.route;
    else if (item.interface_type) window.location.href = `/interface/${item.interface_type === "personal" ? "" : item.interface_type}`;
  }
  return <div className="mie-shell"><style>{`*{box-sizing:border-box}html,body{margin:0;background:#07090d;color:#f5f7fb;font-family:Inter,ui-sans-serif,system-ui,sans-serif}body{overflow-y:auto}.mie-shell{min-height:100dvh;background:radial-gradient(circle at 82% -10%,rgba(86,112,255,.18),transparent 32%),linear-gradient(180deg,#080a0f,#0b0e14 58%,#07090d)}.mie-top{position:sticky;top:0;z-index:10;background:rgba(7,9,13,.86);backdrop-filter:blur(18px);border-bottom:1px solid rgba(255,255,255,.08)}.mie-nav{max-width:1240px;margin:auto;padding:15px 20px;display:flex;align-items:center;justify-content:space-between}.mie-brand{display:flex;align-items:center;gap:10px;color:#fff;text-decoration:none;font-weight:800}.mie-mark{width:34px;height:34px;border:1px solid rgba(255,255,255,.16);border-radius:11px;display:grid;place-items:center;background:#111620}.mie-nav a{color:#929db2;text-decoration:none;font-size:12px}.mie-nav .active{color:#fff}.mie-main{max-width:1240px;margin:auto;padding:42px 20px 80px}.mie-hero{display:grid;grid-template-columns:1.35fr .65fr;gap:22px;align-items:end}.mie-eyebrow{font-size:11px;letter-spacing:.2em;color:#8792aa;text-transform:uppercase}.mie-h1{font-size:clamp(44px,7vw,82px);line-height:.92;letter-spacing:-.065em;margin:12px 0 18px;max-width:850px}.mie-copy{color:#aab3c5;font-size:16px;line-height:1.65;max-width:760px}.mie-note{padding:20px;border:1px solid rgba(255,255,255,.08);border-radius:20px;background:rgba(255,255,255,.035);color:#9ca6ba;font-size:12px;line-height:1.65}.mie-note strong{display:block;color:#f2f4f8;font-size:15px;margin-bottom:6px}.mie-tools{display:flex;gap:9px;flex-wrap:wrap;margin-top:30px}.mie-search{flex:1;min-width:230px;background:#0d1016;border:1px solid rgba(255,255,255,.1);border-radius:13px;color:#fff;padding:12px 14px;outline:none}.mie-filter{border:1px solid rgba(255,255,255,.1);background:#11151d;color:#929db2;border-radius:12px;padding:10px 13px;cursor:pointer}.mie-filter.active{background:#f3f5f9;color:#080a0e;border-color:#f3f5f9}.mie-section{margin-top:38px}.mie-section-title{display:flex;justify-content:space-between;align-items:end}.mie-section-title h2{font-size:22px;margin:0}.mie-section-title span{color:#707b91;font-size:11px}.mie-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-top:16px}.mie-card{min-height:250px;padding:20px;border:1px solid rgba(255,255,255,.09);border-radius:22px;background:linear-gradient(145deg,rgba(18,22,31,.94),rgba(12,15,21,.82));display:flex;flex-direction:column;transition:transform .18s,border-color .18s}.mie-card:hover{transform:translateY(-3px);border-color:rgba(255,255,255,.18)}.mie-icon{width:46px;height:46px;border-radius:14px;display:grid;place-items:center;background:#171d2a;font-weight:800}.mie-badges{display:flex;gap:7px;margin-top:15px}.mie-badge{font-size:9px;letter-spacing:.12em;text-transform:uppercase;color:#8d98ac;border:1px solid rgba(255,255,255,.08);padding:5px 7px;border-radius:8px}.mie-badge.new{color:#f2f4f8}.mie-card h3{font-size:20px;margin:8px 0}.mie-card p{color:#8993a8;font-size:12px;line-height:1.55;margin:0 0 16px;flex:1}.mie-stats{display:flex;gap:14px;color:#7f8aa0;font-size:10px;margin-bottom:15px}.mie-open{border:1px solid rgba(255,255,255,.1);background:#151a24;color:#fff;border-radius:11px;padding:10px;cursor:pointer}.mie-empty{padding:30px;text-align:center;color:#8993a8;border:1px dashed rgba(255,255,255,.12);border-radius:18px;margin-top:22px}.mie-message{position:fixed;bottom:20px;left:50%;transform:translateX(-50%);background:#f3f5f9;color:#080a0e;padding:12px 16px;border-radius:12px;font-size:12px;font-weight:700;z-index:20;max-width:90vw}@media(max-width:900px){.mie-hero{grid-template-columns:1fr}.mie-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:600px){.mie-main{padding:26px 14px 55px}.mie-grid{grid-template-columns:1fr}.mie-h1{font-size:48px}.mie-nav{padding:12px 14px}}`}</style><header className="mie-top"><nav className="mie-nav"><a className="mie-brand" href="/interface"><span className="mie-mark">M</span><span>Merveil Discover</span></a><div><a className="active" href="/interface/explore">Discover</a><span style={{margin:"0 10px",color:"#3b4250"}}>·</span><a href="/interface/store">Store</a></div></nav></header><main className="mie-main"><section className="mie-hero"><div><div className="mie-eyebrow">MERVEIL · DISCOVERY INTELLIGENCE</div><h1 className="mie-h1">See what Merveil is becoming.</h1><p className="mie-copy">One living discovery layer for the Merveil universe. Citizens can discover our platforms, developer infrastructure, Interfaces and services — while developers and organizations can publish new experiences into the same ecosystem.</p></div><div className="mie-note"><strong>Not an advertising feed.</strong>Merveil ranks useful experiences using freshness, activity, engagement and relevance. New Merveil platforms can appear here automatically as the ecosystem grows.</div></section><div className="mie-tools"><input className="mie-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search Merveil" />{CATEGORIES.map(x=><button key={x} className={`mie-filter ${filter===x?"active":""}`} onClick={()=>setFilter(x)}>{x}</button>)}</div><section className="mie-section"><div className="mie-section-title"><h2>{filter === "All" ? "Discover" : filter}</h2><span>{visible.length} experiences</span></div>{loading?<div className="mie-empty">Merveil is discovering what is available…</div>:visible.length?<div className="mie-grid">{visible.map(item=>{const c=item.discovery_category||"Platforms";return <article className="mie-card" key={item.interface_id||item.id}><div className="mie-icon">{ICONS[c]||"M"}</div><div className="mie-badges"><span className="mie-badge">{c}</span>{item.is_new&&<span className="mie-badge new">New</span>}</div><h3>{item.name}</h3><p>{item.description||"A Merveil experience, service or infrastructure capability."}</p><div className="mie-stats"><span>{item.unique_visitors_7d||0} visitors</span><span>{item.visits_7d||0} visits</span></div><button className="mie-open" onClick={()=>open(item)}>{item.cta||"Explore"} →</button></article>})}</div>:<div className="mie-empty">Nothing matches this search yet.</div>}</section>{message&&<div className="mie-message">{message}</div>}</main></div>;
}
